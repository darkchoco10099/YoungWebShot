# 环境变量配置示例
# 复制此文件为 .env.local 用于本地开发

# 生产环境设置
NODE_ENV=production

# Puppeteer 配置
# 在 Cloudflare Pages 部署时跳过 Chromium 下载
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true

# 可选：自定义 Chromium 路径（仅本地开发）
# PUPPETEER_EXECUTABLE_PATH=/path/to/chrome

# 可选：截图超时设置（毫秒）
# SCREENSHOT_TIMEOUT=30000

# 可选：最大并发截图数量
# MAX_CONCURRENT_SCREENSHOTS=3

# 图床服务配置
# 注意：本项目使用外部图床服务 young-image-worker.darkchoco10099.workers.dev
# 截图生成后会自动上传到图床并返回URL，无需本地存储
# 如需使用自己的图床服务，请修改 app/api/screenshot/route.ts 中的上传URL